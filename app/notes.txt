# Windows
pip install pywin32 opencv-python mss

# macOS
pip install opencv-python mss

# Linux
sudo apt-get install wmctrl x11-utils
pip install opencv-python mss

sudo apt-get install -y wmctrl x11-utils xrandr

pip install xcffib python-xlib
sudo apt-get install libxcomposite-dev libxrender-dev